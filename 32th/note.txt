============== Tree in java =============================



Preorder Traversal


Time Complexity --- O(n); 

1)Root
2)Left Subtree
3)Right Subtree

1 2 4 -1 -1 5 -1 -1 3 -1 6 -1 -1





Inorder Traversal


Time Complexity --- O(n); 

1)Left Subtree
2)Root
3)Right Subtree





Postorder Traversal


Time Complexity --- O(n); 

1)Left Subtree
2)Right Subtree
3)Root


Level Order Traversal


Time Complexity -- O(n)




Count of Nodes -- O(n)





Sum of Nodes

Time Complexity -- O(n)




Heigth of a Tree

Time Complexity -- O(n);



Diameter of a Tree

Number of Nodes in the Longest path between any 2 nodes.  

Time Complexity  -- O(n^2);




Subtree of another Tree


Definition for binary tree node

public class TreeNode{
    int val;
    TreeNode left;
    TreeNode right;
    TreeNode(){};
    TreeNode(int val){
        this.val = val;
    }
    ThreeNode(int val,TreeNode left,TreeNode right){
        this.val = val;
        this.left = left;
        this.right = right;
    }
}



class Solution{
    public boolean isIdentical(TreeNode root,TreeNode subRoot){
        if(root == null && subRoot == null){
            return true;
        }

        if(root == null || subRoot ==null){
            return false;
        }

        if(root.val == subRoot.val){

        return isIdentical(root.left,subRoot,left) && isIdentical(root.right,subRoot,right);
        }

        return false;

    }



   public boolean isSubtree(TreeNode root,ThreeNode subRoot){
        if(subRoot == null){
            return true;
        }    

        if(root == null){
            return false;
        }

        if(root.val == subRoot.val){
             if(isIdentical(root,subRoot)){
                return true;
             }
        }

       return isSubtree(root.left,subRoot) || isSubtree(root.right,subRoot);
   }
}


Sum of Nodes at Kth Level 
